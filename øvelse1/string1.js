"use strict";

const fullname = "Albus Percival Wulfric Brian Dumbledore";

let alletegn = fullname.length
console.log(alletegn);

let tegnto = fullname[2];
console.log(tegnto);

let tegnseks = fullname[6];
console.log(tegnseks);

let tegnD = fullname.indexOf("D");
console.log(tegnD);

let laste = fullname.lastIndexOf["e"];
console.log(laste);

// Hvad er det samlede antal tegn, inklusive mellemrum?
// Hvilket tegn er på index 2?
// Hvilket tegn er på index 6?
// Eksperimenter dig frem og find indexet for det første D i Dumbledore
// Lav en beregning ud fra hvad du allerede har fundet, der finder indexet for det sidste e i Dumbledore